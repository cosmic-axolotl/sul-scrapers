# sul-scrapers

O contexto do projeto está em **AGENTS.md**, na raiz. Leia aquele arquivo antes
de qualquer tarefa neste repositório.

Para o Gemini CLI ler AGENTS.md direto, sem passar por aqui, já existe
`.gemini/settings.json` no repo com `context.fileName`.

Não escreva contexto aqui — ele fica desatualizado em relação ao AGENTS.md.
